🍽️ FoodBox: User-Owned Item Listing Platform (Django)This foundational Django web application showcases key full-stack development principles, enabling authenticated users to list, view, and manage their food items.✨ Core Features1. User & Profile ManagementAuthentication: Uses custom registration (forms.py) and Django's built-in LoginView/LogoutView.User Profiles: Implements a One-to-One profile model (Profile) linked to the base User. Signals (post_save) automatically create a profile upon registration and include an ImageField for avatars.2. Item CRUDThe Items model uses a Foreign Key to link each item to its creator.CRUD Operations:Read (List/Detail): Handled efficiently using Class-Based Views (CBVs) (ListView, DetailView).Create: Uses a CreateView that correctly assigns the logged-in user as the item owner.Update/Delete: Implemented using Function-Based Views (FBVs).3. DesignBuilt on Bootstrap 5 with template inheritance (base.html).Utilizes the Messages Framework for user feedback and widget_tweaks for form styling.🛠️ Concepts DemonstratedModels & Relationships: ForeignKey, OneToOneField.Views: Combining Class-Based (ListView, CreateView) and Function-Based Views.Forms: Custom and ModelForm usage.Architecture: URL routing, Namespacing, and Signals.Files: Configuration for MEDIA_ROOT/MEDIA_URL.
